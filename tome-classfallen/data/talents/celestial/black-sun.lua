newTalent{
   name = "Sunset Blade", short_name = "FLN_BLACKSUN_SUNSET",
   type = {"celestial/black-sun", 4},
   require = cursed_str_req_high1,
   points = 5,
   mode = "passive",
   info = function(self, t)
      return ([[In progress...]]):format()
   end,
}

newTalent{
   name = "Devour the Pain", short_name = "FLN_BLACKSUN_DEVOUR",
   type = {"celestial/black-sun", 4},
   require = cursed_str_req_high2,
   points = 5,
   mode = "passive",
   info = function(self, t)
      return ([[In progress...]]):format()
   end,
}

newTalent{
   name = "Singularity Armor", short_name = "FLN_BLACKSUN_SINGULARITY",
   type = {"celestial/black-sun", 4},
   require = cursed_str_req_high3,
   points = 5,
   mode = "passive",
   info = function(self, t)
      return ([[In progress...]]):format()
   end,
}

newTalent{
   name = "Doom Spiral", short_name = "FLN_BLACKSUN_SPIRAL",
   type = {"celestial/black-sun", 4},
   require = cursed_str_req_high4,
   points = 5,
   mode = "passive",
   info = function(self, t)
      return ([[In progress...]]):format()
   end,
}
