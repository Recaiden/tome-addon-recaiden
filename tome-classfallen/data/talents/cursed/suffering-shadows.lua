newTalent{
   name = "Placeholder", short_name = "FLN_SHADOW_TWISTING_SHADOWS",
   type = {"cursed/suffering-shadows", 4},
   require = cursed_wil_req_high1,
   points = 5,
   mode = "passive",
   info = function(self, t)
      return ([[In progress...]]):format()
   end,
}

newTalent{
   name = "Placeholder", short_name = "FLN_SHADOW_MADDENING_SHADOWS",
   type = {"cursed/suffering-shadows", 4},
   require = cursed_wil_req_high2,
   points = 5,
   mode = "passive",
   info = function(self, t)
      return ([[In progress...]]):format()
   end,
}

newTalent{
   name = "Placeholder", short_name = "FLN_SHADOW_GUARDING_SHADOWS",
   type = {"cursed/suffering-shadows", 4},
   require = cursed_wil_req_high3,
   points = 5,
   mode = "passive",
   info = function(self, t)
      return ([[In progress...]]):format()
   end,
}

newTalent{
   name = "Shadow Reave", short_name = "FLN_SHADOW_SHADOW_REAVE",
   type = {"cursed/suffering-shadows", 4},
   require = cursed_wil_req_high4,
   points = 5,
   mode = "passive",
   info = function(self, t)
      return ([[In progress...]]):format()
   end,
}
